name: Terraform Gitbub Actions

on: [push]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: docker://hashicorp/terraform:1.3.5
        with:
          entrypoint: terraform
          args: init
      - run: terraform validate